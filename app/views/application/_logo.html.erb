<div class="logo">
  <%= link_to image_tag("/assets/i-am-mixed-logo.png"), root_path %>
</div>

<div class="color-bar">
  <% if signed_in? %>
    <p class="signed-in">You are signed in as: <%= current_user.email %></p>
  <% end %>
</div>
